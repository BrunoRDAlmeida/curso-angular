"use strict";

const container = document.querySelector("container")
// container.append("<p> Olá Mundo </p>")

const newDiv = document.createElement("div")
newDiv.innerText

containder.appendChild(newDiv);
